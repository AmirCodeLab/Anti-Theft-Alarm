<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Only carries the app through to the first Compose frame; the palette lives in ui/theme. -->
    <style name="Theme.Guard" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/marine</item>
    </style>
</resources>
