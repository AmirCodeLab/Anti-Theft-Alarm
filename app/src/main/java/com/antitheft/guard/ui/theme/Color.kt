package com.antitheft.guard.ui.theme

import androidx.compose.ui.graphics.Color

/** Deep marine base the whole app sits on. */
val Marine = Color(0xFF101A24)
val MarineRaised = Color(0xFF16232F)
val MarineOutline = Color(0xFF243544)

/** Amber is reserved for the armed state and nothing else. */
val Amber = Color(0xFFFFB020)

/** Red is reserved for an alarm actually firing, so it never loses its meaning. */
val AlarmRed = Color(0xFFFF5A5F)

val TextPrimary = Color(0xFFE8EEF4)
val TextMuted = Color(0xFF8CA0B3)
